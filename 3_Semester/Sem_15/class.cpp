#include<iostream>
#include<string>
using std::cout;
using std::endl;
//namespace::std;

template <typename T>
T abs(T x) {
    if (x < 0)
        return -x;
    return x.abracadabra();
};

int main() {
    //cout << abs(-5) << endl;
}